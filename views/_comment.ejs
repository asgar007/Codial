<li>
    <p>
        <%= comment.content %>
        <br>
        <small>commented by
            <%= comment.user.name %>
        </small>

        <% if(locals.user && locals.user.id == comment.user.id)  {  %>
            <small>
                <button><a href="/comments/destroy/<%= comment.id %>">Delete comment</a></button>
            </small>
        <% } %>
    </p>
</li>